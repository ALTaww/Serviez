import { CreateServiceForm } from "../components";

const CreateService = () => {
  return (
    <div className="page create-service-page">
      <CreateServiceForm />
    </div>
  );
};

export default CreateService;
